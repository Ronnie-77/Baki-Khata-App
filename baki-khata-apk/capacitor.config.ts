import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.bakikhata.app',
  appName: 'বাকি খাতা',
  webDir: 'out',
  server: {
    androidScheme: 'https'
  }
};

export default config;
